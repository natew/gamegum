    <ul class="nav">
      <%= li 'Forum Home', { :controller => 'topics', :action => 'index' }, :active => { :when => [[],['index']] } %>
      <%= render :partial => 'topics/topic', :collection => @topics %>
    </ul>
