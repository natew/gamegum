    <%= form_for :post, :url => { :controller => 'posts', :action => 'create', :discussion => @discussion.id } do |form| %>
      <%= form.text_area :message, :rows => 6 %><br />
      <%= submit_tag "Reply", :class => 'submitbutton right' %>
    <% end %>